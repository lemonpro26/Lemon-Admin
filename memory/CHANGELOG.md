# Lemon Pros — Changelog

## 2026-07-13j (fork continuation)
- **Resolve the ad an old/renamed lead came from:** New `fetch_ad_by_id` + `GET /admin/google-ads/ad-lookup?ad_id=` resolves any Google ad by ID regardless of status (ENABLED/PAUSED/REMOVED), returning current name, size, type, status, ad group, campaign (only 74 of 1,495 ads are enabled, so the live name cache misses paused/renamed ones). Lead detail dialog now auto-resolves the ad by ID on open and shows the real name + an "Ad status" row when paused/removed. Verified on a paused ad.

## 2026-07-13i (fork continuation)
- **Analytics tab load delay fixed (caching):** The delay was live Google Ads API calls made on every open. Added (1) an in-process **OAuth access-token cache** (55-min TTL — was minting a fresh token on every Google call, app-wide) and (2) a **5-min report cache** for `fetch_spend_by_campaign`, `fetch_spend_by_day`, and `fetch_sitelink_metrics`. Result: `/admin/analytics` 0.72s→0.16s and `/admin/google-ads/sitelinks` 1.58s→0.18s on warm loads. Cold (first open per 5 min) still refreshes from Google.

## 2026-07-13h (fork continuation)
- **Display leads show ad Size instead of Keyword:** `google_names_service.fetch_names` now also pulls image-ad pixel dimensions and builds an `ad_size` map `{ad_id: "336x280"}` (stored in `ad_labels.ad_size`, merged on sync). `_resolve_ad_names` attaches `ad_size` to any lead/call whose `ad_id` (captured via ValueTrack `{creative}`/`sub2`) is a sized image ad. Lead detail dialogs (Leads tab + shared Retained dialog) render a **Size** row instead of **Keyword** whenever `ad_size` is present. Verified: a Lemon Display lead (ad_id 816687329475) resolved to Size 300×250, Keyword row hidden; search leads still show Keyword.

## 2026-07-13g (fork continuation)
- **Auto-populating campaign filters on top bars:** Calls tab gained a new "CAMPAIGN" chip row (all enabled Google campaigns ∪ any present in data — incl. Lemon Display 2026 — with unique-caller counts, click to filter). Leads tab campaign dropdown now unions in all enabled campaigns too (Lemon Display shows even at 0). Both fetch from `/admin/campaigns` (enabled-only), so newly enabled/added Google campaigns appear automatically with no hardcoding. Verified via screenshots on both tabs.

## 2026-07-13f (fork continuation)
- **Delete/clear campaign:** Campaign editor (inline `CampaignCell` + detail-dialog `CampaignEditor`) now shows a trash icon when a campaign is set. Clearing sets a `campaign_cleared` flag so the attribution backfill (incl. the Demand Gen rule) won't silently re-fill it. Backend `POST /admin/{kind}/{id}/campaign` accepts `{clear:true}`. Verified: cleared a call → stayed empty through a backfill run.
- **Lead origin audit (read-only) — answers "did you import from Quickbase?":** Confirmed the app NEVER imports leads from Quickbase. Leads are created ONLY by the landing-page form (`POST /leads`) or the manual "Submit Test Lead" button; calls only by the CTM webhook. Quickbase is read-only (name/email/city lookup by phone). New `GET /admin/leads/origin-audit` classifies every lead as landing_page / test / no_origin and lists any no-origin records. Verified on preview: 6 leads → 5 landing_page, 1 test, 0 no_origin. The "Cassandra Jones (LL)…" name is a Quickbase-enriched display name on a real phone-matched record, not a phantom import.

## 2026-07-13e (fork continuation)
- **Inline campaign edit UX fixes:** (1) Saving no longer reloads the whole table — `CampaignCell` keeps a local `current` value and parents patch their list state in place (`setCalls`/`setData`), so it "just updates". (2) Clicking edit no longer scrolls/jumps the page — replaced `autoFocus` with `inputRef.focus({ preventScroll: true })`. Verified: edited a call's campaign inline; cell + badge updated instantly, no reload, no jump.

## 2026-07-13d (fork continuation)
- **Demand Gen source → Demand Gen campaign mapping:** Calls/leads that came in through a Demand Gen landing page or tracked number now attribute to their real Google campaign — `/dg` + number 833-240-9312 → "Demand Gen 2026", `/dgs` + 833-868-1802 → "Demand Gen 2026 Spanish". Added `_demandgen_campaign_name` + a final fallback in `_derive_campaign_id` (fires only when no other campaign resolved, so it fills blanks without clobbering real Google matches). Backfill projection widened to include `number_group`/`source_page`; new webhook calls get it automatically. These campaigns are paused (hidden from the manual picker) but still resolve for display. Verified: a dgs-sourced call backfilled to "Demand Gen 2026 Spanish".

## 2026-07-13c (fork continuation)
- **Inline campaign editing in tables (no dialog needed):** New compact `CampaignCell` component added to the **Calls** and **Retained** table Campaign columns. Hover reveals a pencil → inline datalist (pick from enabled Google campaigns or type custom) → save, all without opening the detail dialog. Click propagation is stopped so editing never triggers the row's open-dialog onClick. Verified: edited a call's campaign inline → "Campaign updated" toast, cell + badge + Attributed filter all refreshed. (Leads table has no Campaign column, so nothing there.)
- **Picker restricted to ENABLED campaigns:** `/admin/campaigns` now intersects the cumulative `ad_labels.campaign` map with `live_campaigns`, so only currently-enabled campaigns are offered (10, matching the owner's Google Ads screenshot) — paused/removed ones (Demand Gen 2026/Spanish, Lemon Pmax for Seniors, The Lemon Law Lawyers) no longer appear.
- **Campaign filters sort blank → A→Z:** Retained "by campaign" chips and Leads "All campaigns" dropdown now list the blank/Unattributed bucket first, then alphabetically.

## 2026-07-13b (fork continuation)
- **Retained — real city/state from Quickbase:** The Quickbase lookup now also pulls the client's City (field 12) + State/Region (field 13) and `_enrich_from_quickbase` persists them onto the lead/call (`city`/`state` + `qb_city`/`qb_state`), overriding CTM geo/IP-derived location. The Retained tab self-heals any item missing a `qb_city` on load, and the "Sync from Quickbase" button backfills all. Added `QUICKBASE_FIELD_CITY="12"` + `QUICKBASE_FIELD_STATE="13"` env (multi-choice state values flattened via `_flatten`). Verified end-to-end: a lead seeded with WRONGCITY/ZZ resolved to the real "Montebello, California" from Quickbase. NOTE: these two env vars must also be set in the production deploy env.
- **Campaign picker excludes [E]/[M] variants:** `/admin/campaigns` now filters out campaigns whose name ends in `[E]` or `[M]` (Enhanced/Manual variants the owner doesn't use) — 46 → 11 usable campaigns in the manual picker.

## 2026-07-13 (fork continuation)
- **Manual Campaign editor in Lead/Call detail dialogs:** Wired the `CampaignEditor` component into `LeadDetailDialog.jsx` and `CallDetailDialog.jsx` (replaces the read-only Campaign row). Admins can pick from the Google campaign list (native datalist) or type a custom campaign, then save. Backend `POST /admin/{kind}/{item_id}/campaign` resolves name→id via `_campaign_name_to_id` and persists `campaign_id`/`campaign_name`/`campaign_manual`. Verified end-to-end: edit → "Campaign updated" toast → value + Retained campaign chip update.
- **Retained tab — Columns show/hide toggle:** Added a "Columns" DropdownMenu (checkbox items) to the Retained toolbar to toggle Type / Phone / Source / Campaign / Revenue / Came-in columns (Client / Retained on / Detail always shown). Verified via screenshot.
- **Retained tab — "Retained by City" breakdown:** New city filter chips (city + state, count, revenue) mirroring the campaign breakdown; clicking narrows the table + stats. Backend `/admin/retained` now returns `city`/`state` on lead items too (calls already had them). Verified: Miami FL ($5,000) + Houston TX chips render and filter.

## 2026-07-10 (fork continuation)
- **Widened call-match window to ±6h (fixes long-call/PMax misses):** Screenshot of Google's Call details report confirmed (a) PMax calls DO appear in call_view with their campaign, and (b) Google truncates every call's start time to the hour. Long calls (e.g. 2145s) drift >90min from the truncated time, so the ±90min window still rejected them. Widened `_GCALL_TIME_WINDOW_S` to 21600s. Match is now effectively area code (exact) + closest duration (primary) + time (tiebreaker) — robust to Google's time truncation.
- **Performance Max & any-source call attribution via raw CTM payload:** PMax campaigns (e.g. "Lemon Pmax") never appear in Google's click_view (no gclid data) or call_view, so neither exact nor fuzzy Google matching can see them — the campaign only lives in the CTM record. Added `_deep_find_value` recursive search of the stored raw CTM webhook payload for the campaign name (any field name / nesting), wired into `_derive_campaign_id`, the webhook ingest, and the historical backfill. Now a call whose campaign is buried anywhere in `raw` gets attributed. Verified: a PMax-style call with the campaign only in a nested raw field resolves to its campaign_id.
- **Duration-primary call matching:** For calls without a gclid, the call_view→CTM matcher now uses match key = area code (hard filter) + CLOSEST duration (primary) + closest time (tiebreaker), within a ±90min window. Duration is a near-unique fingerprint so it disambiguates multiple same-area-code calls in the same hour (Google truncates call time, so time alone is unreliable). Verified via unit test: closest-duration row wins even when further in time.
- **Exact gclid→campaign call attribution (definitive fix):** Confirmed via web research that the Google Ads API does NOT expose the full caller number (only area code) — and the call_view start time is truncated (often to the hour), which is why ±15min area-code matching kept missing calls. New approach: `google_names_service.fetch_gclid_campaigns` queries `click_view` day-by-day to map every gclid→campaign (verified: 93,194 gclids from the live account). `_enrich_calls_with_google` now attributes any call carrying a gclid EXACTLY by gclid first (no fuzzy), then falls back to area-code+time matching with the window widened to ±90min and duration as a soft tiebreaker. gclid fetch scoped to the sync window (light for the 20-min pass, full 90-day map on the daily pass). Returns `gclid_matched`/`gclids_seen` counts.
- **Google call matching relaxed + auto full-history attribution:** The Google call_view→CTM matcher used a hard ±25s duration filter that rejected valid matches (CTM vs Google report different durations), leaving calls like (760) 684-7953 unattributed despite Google having the campaign. Removed the hard duration reject — duration is now a soft tiebreaker; matching is area code + time (±15min) with closest-match scoring. Also the `_google_call_sync_loop` now runs a FULL historical re-scan once a day (and on boot), not just a 3-day pass, so all past calls get attributed automatically as Google data catches up. Verified the full pass hits the live Google API (98 call_view rows). Removed unused `_GCALL_DURATION_TOL_S`.
- **Calls/leads no longer left "unattributed" when the campaign is known:** Attribution keys off numeric `campaign_id`, but CTM sends a free-text campaign NAME (e.g. "Lemon Law 2026") and the Google match wrote only to `google_campaign_id` — so calls showed unattributed. Added `_campaign_name_to_id` (reverse map from synced ad_labels) + `_derive_campaign_id` (explicit id → google_campaign_id → reverse-mapped name). Now: (1) CTM webhook resolves campaign_id on ingest (broadened campaign-name keys incl. utm_campaign/google_campaign), (2) `_enrich_calls_with_google` sets campaign_id from the Google match, (3) `_backfill_attribution()` fills past calls & leads and auto-runs in the 20-min call-sync loop. New admin endpoint `POST /admin/attribution/backfill` + folded into `POST /admin/calls/sync-google`. Verified: a call with campaign "Lemon Law 2026" and no id → campaign_id 23956437547.
- **Channels tab — click a network to see spend by campaign:** Network cards are now clickable and expand a "Spend by campaign" panel (campaign name + amount + proportional bar, sorted high→low, with total). New backend endpoint `GET /admin/channels/campaigns?network=&start=&end=` returns Google campaign spend (from `fetch_spend_by_campaign` + resolved names); non-live networks (FB/IG/Native) return an empty list with a "not tracked yet" message. Drill-down resets on date-range change. Verified via curl (35 real Google campaigns) + screenshot.
- **Retained tab — "Retained by campaign" breakdown + filter:** Added a campaign breakdown to the Retained area (`AdminRetained.jsx`): clickable chips per campaign showing retained count + revenue, an "All campaigns" chip, and a new "Campaign" column in the table. Clicking a chip filters the table AND the stat cards (like the network chips). Unattributed/direct retained bucket into "Unattributed / Direct". Campaign IDs resolve to names via existing `_resolve_ad_names`. Verified via screenshots (chips render, click-to-filter narrows table + stats).
- **Quickbase alt-phone made deployment-proof:** Added explicit `QUICKBASE_FIELD_PHONE_ALT="47"` env var + `_cfg` support so the alt "Alt Phone Number" field is ALWAYS searched even if the runtime fields-API discovery call fails on production (the suspected cause of the name flipping back to "SANTA ROSA CA (Duplicate)"). Hardened `_phone_field_ids` to not cache failed discovery and to log discovered fields. Full chain now bulletproof: alt-field search (config + discovery) → `_best_match` skips "(Duplicate)" → Retained tab self-heals junk names on load. Verified end-to-end through the live server. NOTE: requires `QUICKBASE_FIELD_PHONE_ALT` to also be set in the production deploy env.
- **Retained tab self-heals junk/duplicate names:** The Retained endpoint only re-looked-up QB names when a record had NO name, so a record already stuck on a junk value ("SANTA ROSA CA (Duplicate)") never refreshed. Now it re-looks-up any item whose name is missing OR contains "(Duplicate)". Combined with the deterministic `_best_match` lookup, stuck names auto-correct on Retained tab load. Verified end-to-end in preview: a call stuck on "SANTA ROSA CA (Duplicate)" auto-corrected to "Jessica Martinez Escarcega".
- **Quickbase name flip-flop fixed (duplicate records):** When several QB records share a phone number, `lookup_by_phone` used `top:1` with no ordering, so a retained client's name randomly flipped between the real record and a junk "(Duplicate)" record (e.g. (707) 852-9185 → "Jessica Martinez Escarcega" vs "SANTA ROSA CA (Duplicate)"). Now fetches all matches (top:25, deduped by record id) and picks the best via `_best_match` scoring: skips "(Duplicate)" names, prefers records with an email and a proper mixed-case name. Deterministic — verified 3/3 returns "Jessica Martinez Escarcega". Existing records self-heal on next sync.
- **Funnel Analytics now counts ALL landing pages:** `/admin/funnel` and `/admin/funnel/campaigns` previously recognized only home/lapa/sp/laspa and dumped everything else (Demand Gen /dg, /dgs and Team /tm, /tm2) into "home", and didn't canonicalize aliases (/dg stores `ladg`). Fixed to canonicalize via `_canon_page` + reverse-alias map `_CANON_TO_ALIASES` and expose all 8 pages (Home, PA, PA Spanish, Spanish, Demand Gen, Demand Gen Spanish, Team Overlay, Team Split). Added matching tabs in `AdminFunnel.jsx`. Verified via curl + screenshot.
- **Quickbase lookup now matches secondary/alt phone numbers:** `lookup_by_phone` only searched the primary "Phone Number" field (fid 7), so retained clients whose call number is stored under "Alt Phone Number" (fid 47) never got their name auto-filled. Added auto-discovery of ALL phone-type fields via the QB fields API (cached) and search across them with OR. Verified live: (707) 852-9185 now resolves to "Jessica Martinez Escarcega". Existing records self-heal on the next hourly sync / "Sync now" / Retained tab reload.
- **Landing pages grouped by shared phone number (expandable):** Pages that share a tracked number are now combined into one row and their stats aggregated. Groups: "Home, PA & Team" (844-335-8911 → /, /pa, /tm, /tm2), "Spanish (/sp + /spa)" (866-524-3722), "Demand Gen (/dg)" (833-240-9312), "Demand Gen Spanish (/dgs)" (833-868-1802), plus "Direct / Untracked". Each combined row is expandable (chevron) to reveal per-page visits/leads/calls/retained/financials. Backend `_page_group`/`_GROUP_META` + `_group_landing` in `/admin/analytics`; frontend `DrillTable` gained `expandable` with nested child rows. Verified via seeded multi-child data + screenshot; test data cleaned up.
- **Duplicate landing-page rows FIXED (data bug):** The same page was stored under alias `source_page` codes (`dg`/`ladg`, `tm`/`latm`, `pa`/`lapa`, `spa`/`laspa`) which aggregated as separate rows. Added `_canon_page`/`_PAGE_ALIASES` and canonicalized clicks/leads/calls/revenue/retained/spend in `/admin/analytics` so each page yields exactly ONE row. Fixed `GROUP_PRIMARY_PAGE` to use canonical codes (`dg`/`dgs`). Verified: testing_agent iteration_24 13/13 pass, no duplicate/alias codes remain.
- **calls > visits on /spa — explained (not a bug):** The Spanish tracked number (866-524-3722) is SHARED by both /sp and /spa. Calls with no page visit/tap default to the group's primary page (/spa), so /spa absorbs Spanish-number calls beyond its own visits. Known attribution heuristic.
- **By Campaign retained reconciliation:** Retained was counted by `created_at` (leads/calls created in range) but the Retained tab counts by `retained_at`. Fixed `/admin/analytics` to count retained by `retained_at`, attribute to campaign/landing page (calls enriched for click+tap attribution), and roll retained with no campaign (or paused campaigns not shown) into a new **"Unattributed / Direct"** row so the Retained column sums to the Retained tab total. Verified (both = 2 in preview).
- **Totals row in Analytics tables:** Added a `showTotals` footer to `DrillTable` (`AdminAnalytics.jsx`). By Landing Page and By Campaign now show a bottom **Total** row. Additive columns (Visits/Leads/Calls/Spend/Revenue/Retained) are summed; ratio columns (Conv. Rate, ROAS, CPL, CPA, Bounce Rate) are recomputed from the aggregates (weighted), not naïvely summed. Verified via screenshot.

## 2026-07-09 (fork continuation)
- **Analytics — Landing Page "Retained" column (frontend):** Added `Retained` to `FIN_COLS` so it shows on By Landing Page and By Campaign.
- **CPL / CPA math confirmed:** CPL = Spend ÷ (Leads + Calls), CPA = Spend ÷ Retained; null when spend=0.
- **Defensive read-time call filter:** `/admin/calls` filters out `number_group == 'other'` (untracked numbers).
- **Regression:** testing_agent iteration_23 — 11/11 backend pass. `total_retained` lives on `/admin/stats` (not `/admin/metrics`).

### Still pending / backlog
- P1: Make FB/IG/Native channels real (capture fbclid/ttclid, save network, live data)
- P1: Hook-performance-from-calls view
- P2: Lead-to-revenue pipeline statuses (New → Contacted → Retained)
- P2: Refactor server.py (>4,100 lines) into routers
- P3: Conversions by Sitelink, lead qualification logic, "Unattributed/Direct" filter chip in Calls/Leads, live-call webhook view
- P3 (blocked): SMTP email notifications (need creds)
- Top Tabs UI mockup at `/mockup/tabs` — user chose "don't do this yet"
